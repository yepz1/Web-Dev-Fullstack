var colors = ["red", "green", "blue"];

//adds new element to the end of the array
colors.push("yellow");

//removes the last element of the array
colors.pop();

//adds new element to the beginning of the array
colors.unshift("purple");

//removes the first element of the array
colors.shift();

//returns the index of the element
colors.indexOf("red");

//copy some elements from the array
var newColors = colors.slice(1, 3);

//copy all elements from the array
var otherColors = colors.slice();



var todos = ["Buy New Turtle"];
window.setTimeout(function() {
  // put all the rest of your JS code from the lecture here
}, 500);
